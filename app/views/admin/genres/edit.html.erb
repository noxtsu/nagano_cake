<h3 class="text-black-50">ジャンル編集</h3>

<div slass="container">
  <div class="row">
    <div class="col-sm-auto">
      <%= form_with model: @genre, url: admin_genre_path, local: true do |f| %>
      <h4>ジャンル名</h4>
      <%= f.text_field :name %>
      <%= f.submit '新規登録' %>
      <% end %>
    </div>
  </div>
</div>
